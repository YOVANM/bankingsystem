spring:
  application:
    name: bankingsystem
  datasource:
    url: jdbc:mysql://localhost:3306/bankingsystem
    username: YOVANM
    password: personal543R$
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html